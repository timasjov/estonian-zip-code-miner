# Estonian ZIP code miner #
Scala script for mining Estonian ZIP codes by address. Add `lib/*.jar` to classpath and run `scala grab.scala`.

### Dependencies ###
Selenium server: `org.seleniumhq.selenium:selenium-server:2.48.2`